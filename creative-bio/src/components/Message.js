const Message = ({greet, name}) => {
    return <span>{greet}, {name}!</span>;
};

export default Message;

//this is an example of react PROPS 
//make function read props - make props as a parameter
//this can create personalised messages