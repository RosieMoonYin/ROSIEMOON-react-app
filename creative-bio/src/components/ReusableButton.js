const ReusableButton = ({children}) => {
    return <button className="="reuse-btn>{children}</button>;
};


//children is special prop!
export default ReusableButton;

//props make messages dynamic :)